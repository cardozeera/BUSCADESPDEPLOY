services:
  - type: web
    name: buscadesp-backend
    env: python
    pythonVersion: 3.11
    buildCommand: |
      pip install -r requirements.txt
      playwright install
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
